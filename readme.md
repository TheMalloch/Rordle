# Projet Wordle V0.01

## Features:
    - Selecte a random word of five letters from a document or a list* 
    - Able to play the game in local or via an API
    - This is a Wordle so the usual game mecanics
    - Servers to be host and request at by other player
    - Words verification (local or scrap)
    
### Word selection : 
    Each player will have the same word until they all solve it, attempt will be limited to x entry of five letter words.
    The words chooses come from two sources : File on the server or scraping a random website and selecting a random or first five letter word in the <p></p>
    
### API
    - Word of the day
    - Request token (need to provide a username when using it)
    - The response will be made with match and enum (using an array)
    - token for one user in to requesting sous token or token per session
    - As the I working on it.
    
### Server
    - Make it or no game
